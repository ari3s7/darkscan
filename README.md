# DarkScan

Automated website dark-pattern auditor.

## Layout

- `apps/web` — React + TypeScript
- `apps/server` — Express + TypeScript
- `packages/rules` — dark-pattern category definitions

## Prerequisites

- Node.js 20+
