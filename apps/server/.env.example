PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/darkscan?schema=public
CORS_ORIGIN=http://localhost:5173
CRAWL_TIMEOUT_MS=30000
# true only while scanning the local demo site. Leave false if this API is reachable by anyone else.
CRAWL_ALLOW_PRIVATE=false
# Use one provider. OpenRouter takes precedence when its key is set.
OPENROUTER_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
